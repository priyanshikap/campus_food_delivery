// Re-exported for import ergonomics — `useCart` from "../hooks/useCart"
// and from "../context/CartContext" are the same hook, single source of truth.
export { useCart } from "../context/CartContext";
