// Re-exported for import ergonomics — `useAuth` from "../hooks/useAuth"
// and from "../context/AuthContext" are the same hook, single source of truth.
export { useAuth } from "../context/AuthContext";
